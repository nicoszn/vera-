{
  "name": "autonomous-mcp-workspace",
  "private": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push"
  },
  "devDependencies": {
    "prisma": "^7.9.1"
  }
}
