{
  "name": "autonomous-mcp-workspace",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push"
  },
  "devDependencies": {
    "prisma": "^5.11.0"
  }
}
